### Keynote: Rust, in Sickness & in Health
What does it actually mean to bet a company on a particular programming language? Seven years ago, Bryan Cantrill helped start Oxide Computer Company with Rust at the center of that bet, and spent the years since finding out the answer to this question.
In this talk, Bryan will share how his thinking about Rust has evolved, from the earliest infatuation to the honeymoon to the many ups and several downs of a long, deep relationship with the language, tested across bootloaders, an operating system, a hypervisor, a distributed storage system, a network operating system, compilers, and debuggers. Bryan will get into where he thinks Rust is strong, where it still has real gaps, and what he thinks the language and ecosystem need to prioritize as Rust moves further into systems’ future.

### Sponsored Lightning talk - Microsoft: Rust (in production) Without Fear
* Rust sits among C C++ C# TypeScript as the best supported lang for internal development at Microsoft
* What does it take to make Rust prod-ready at MS
* Great Windows system comaptibility

- Paved path for Rust
* 1ES
* Crate security 
* Micorsoft Rust SDK
* Agentic workflows

- codegen_utc
* the need for a windows first compiler codegen for rust
* MSVC is the platform compiler for Windows
* same interface rustc uses for LLVM GCC
* Native platform evolution
* seamles Rust /C++ interop for hybrid proj
* Post link compliance
* cross language
* binary hardening code security

### Sponsored Lightning talk - Peeriot: Myrmic: An Open Rust Runtime for Distributed Edge Applications
Myrmic is an open-source runtime for distributed edge applications. It provides execution, messaging, state handling, and placement for application logic running across heterogeneous edge and embedded devices, without requiring a central cloud service in the control path.

This lightning talk introduces two core aspects of the system. First, Myrmic provides a complete runtime stack for building and coordinating distributed applications across the edge. Second, its dual-engine architecture separates portable, sandboxed application logic from device-specific native Rust code, allowing applications to span gateways and microcontrollers without hiding hardware capabilities behind a lowest-common-denominator abstraction.

In five minutes, attendees will get a compact view of the overall stack, the dual-engine design, the capabilities it enables, and the trade-offs it introduces. Myrmic is open source, and the implementation will be available after the session.


### Project Update: The safety - Critical Rust Consortium Two Years in
The Safety-Critical Rust Consortium (SCRC) was kicked off two years ago, amazingly enough, at RustConf 2024 right here in Montreal. In this talk Pete will recap (briskly!) the last two years of the SCRC, the great work that's been accomplished and the road that lies ahead for getting Rust into more planes, trains, and automobiles. Rust has earned respect in helping to improve the quality of software through its memory-, type-, and thread-safety without doubt. However, there do remain regulatory and compliance items which the SCRC is actively working together on to expand the usage of Rust to safety-critical domains and higher levels of safety-criticality.

[Are we safety critical yet?](https://arewesafetycriticalyet.org/)

https://rfcs.awaresafetycriticalyet.org

[safety-critical-rust-consortium](https://github.com/safety-critical-rust-consortium)
### Sponsored Lightning Talk - Dash0: The Power of Profiling: How Fixing One Async Antipattern Shaved Off 30% of Our Write Latency
Dash0's Great Lakes database was overusing tokio::task::block_in_place, which accounted for a surprisingly large amount of both CPU usage and write latency. In this talk, Brennan will discuss how we pinpointed the issue using our Continuous Profiling product, how we fixed it, and what it means for other resource-conscious Rust users.


### Sponsored Lightning Talk: OneChronos - Rust in Finance
 Using Rust to build the next generation of trading systems and ensure that they are fast, secure, and correct. 

### Project Goals: Funding :: Chocolate : Peanut Butter
Project Goals and funding are two good things that are far better together. The Rust Foundation and the Rust Project are working together to build a sustainable pipeline that turns the growing demand for Rust into increasing funding, both for general maintenance and for important project goals (pun definitely intended). Niko will walk through the pieces already in place, from the Foundation's Maintainer Fund to the Maintainer-in-Residence program, and how the Project Goals program helps the whole community, funders and maintainers alike, align on priorities to

### Sponsored Lightning Talk - Google: How We Replace Common C and C++ Libraries with Rust at Scale
Google depends on a lot of C and C++ libraries for fundamental tasks like image and audio decoding. Unfortunately, due to the nature of C and C++ these libraries are suspect to a large amount of memory safety vulnerabilities and are generally considered unsafe. To address this security risk, our team at Google moved all these common parsing and decoding libraries over to Rust alternatives. We use Crubit to expose a C++ interface for these libraries and then change the callsites to use the Rust alternative. We will talk about a wrapper for image-rs as an example for our efforts

### Making GPUs Feel Native in Rust
GPUs power AI, graphics, scientific computing, and an increasing share of modern software. Many projects focus on making GPU programming in Rust possible. This talk asks a different question: how do we make GPU programming feel native to Rust?

Christian Legnitto, founder and CEO of VectorWare, explores what it would take to make GPUs feel natural extension of the Rust language and ecosystem. The talk covers the challenges and design tradeoffs involved in bringing Rust's ownership model, type system, and abstractions to heterogeneous systems. It also looks at compiler architecture, intermediate representations (IRs), runtimes, and the fundamental differences between CPU and GPU execution.

Attendees will leave with a framework for thinking about how GPU and CPU software interact, an understanding of today's tradeoffs, and a vision for how Rust can make heterogeneous programming feel like ordinary systems programming. 

- Rust's desing supports weird stuff:
1. abstract as musch as you can with zero cost
2. provide opt-in abstraction escapes

- Ecosystem benefits
* The same code gets maximun GPU concurrency and maximum CPU concurrency
* No ecosystem split

- Rust first design for GPUs is bidirectional
* GPU needs may reveal better general abstractions
  * DisjointSlice
  * Data-parallel apis
  * Tensors?
  * matrix multiplication?
* GPU-shaped does not mean GPU specific

vectorware.com/blog



### Rust Atomics in the Wild / Martin Ombura Jr.

He used Rust first for work on his thesis on evolutionary algorithms!

Talk will have an overview of Rust's Atomic Memory Model as well as some specific patterns

Current state of concurrency in Rust: if you need concurrency, add Arc<Mutex>

Rust atomic memory model
* Borrows from C++ 20 atomics
* atomic variables impl Sync.  They are guaranteed to be lock-free but not wait-free
* atomics have a defined set of operations that either load, store, or Read-Modify-Write
* memory ordering: a contract around an atomic operation
* atomic type: fence

Modification order: all modifications occur in a total order that is specific to the variable

There is an [Ordering enum](https://doc.rust-lang.org/std/sync/atomic/enum.Ordering.html) with things like `Acquire` and `SeqCst`

Pattern 1: Acquire-Release Publication
Pattern 2: Prove, Validate, Execute
Pattern 3: SeqCst and single Total Order(S)
   
SeqCst is complex, not an easy fix.  It combines atomicity, synchronoization effects of Acquire for loads, Release, Acquire + Release, Global effects of one

Rayon is a good example
* Rayon has a job queue
* A scenario where the workers are sleeping vs. the producer is sleeping.  Can the producer fail to notice the sleeper?
* Can the worker fail to notice the producer's job?
* Every thread must agree on the final state, "it can't be like a Spiderman multiverse"
* Rayon has a nice Readme that explains the above


Coherence-Ordered before -- it implements constraints

CoB : Rule 1
store value , read value

### Servo DevTools: Where Rust Meets Javascript
Servo is a web rendering engine written in Rust, one of the largest and oldest Rust projects out there. One of the areas we’re actively pushing forward is DevTools support, connecting Servo to Firefox DevTools over the Remote Debug Protocol. This means writing a server in Rust that speaks with a client written in Javascript.

Every browser engine needs DevTools, but building them is a project in itself. In this talk we’ll look at what building DevTools for Servo looks like. The Inspector, the Console, the Debugger. What are the hard parts and what challenges we face. How we navigate a client written in JS, a server written in Rust, and the role SpiderMonkey(C++) and mozjs(Rust) play in making it all work. 


### Real Time Rust in Robotics
Robots are complex interconnections of subsystems which use a varied mix of software. David will describe a typical robot's architecture and highlight the areas where Rust is making headway. He will present his team's experience with bringing Rust to the low-level sensors and actuators on a research robotics platform, showing how the use of bare-metal Rust improved the system's reliability and real-time performance. Finally, he will suggest how the Rust community can encourage further adoption of Rust in robotics and related applications. 

Typical Robot: Camera and or Lidar - Perception, Planning, Control, Message Bus -> Interface - (Part 2 - realtime)Motor Firmware, Sensor Firmware

- Articles:
A first look at ROS 2 applications written in asyncronous Rust
Current state of Rust client libraries

- Pure robotics frameworks
* Hiroz zettascalelabs.github.io/hiroz
* Horus horusrobotics.dev
* Copper copper-robotics.com
* Dora dora-rs.ai

- Inter language bindings
* C: ABI compatible (bindgen)
* Python: Onemain option pyo3
* C++: Mnay interesting options

Part 2: Real-time Embedded
KB, not MB
μs ,not ms
C , not C++
Interfaces

C (zero ways to do what you want, workarounds have poor ergonomics) 
- Rust (One way, libraries are composable) - C++ (Many ways to do what you want )

- Zero-one-many : Memory safety
C: very very careful
Rust: lifetimes & owbership
CC: 

- Zero-one-many : Error handling
C: return codes or global flags
Rust: Result type and ? operator
C++: throwing exceptions OR std::expected<T,E> OR ..

- Zero-one-many : Concurrency
* C: manual state machine polling
* rust: async with executor
* C++: std::async()C++11
 OR co_await coroutines (C++20) or jsut doing it like C

- Zero-one-many : Composability:
* C/C++ RTOS:isolated bugs within processes
, preeemtive multitasking

*bare-metal Rust:
  * prevents bugs at compile time
  * cooperative multitasking
  * crate embedded-hal (extraordinary composability), embassy-executor : bare -metal statically allocated async executor


### Writing a Zero-Copy Database Parser from the Ground Up
Building a high-performance database proxy demands wire-speed parsing with minimal overhead. This talk walks through the design and implementation of a zero-copy streaming parser in Rust that parses database wire protocols incrementally from the first byte received, using a fixed-size ring buffer and arena-allocated borrow tracking to yield structured slices directly into the network buffer with no intermediate copies and constant memory regardless of payload size.

The talk covers the core abstraction: a trait-based streaming API that uses Rust's type system to bind borrowed slices to the underlying ring buffer at compile time, a ring buffer designed to allow concurrent immutable borrows of disjoint regions across async yields, and a borrow tracker that manages lifetimes as the buffer is refilled. Attendees will see how a small ring buffer can parse and inspect every byte of an arbitrarily large payload, how deterministic pattern matching eliminates backtracking on partial reads, and where carefully scoped unsafe makes the design sound.

- What is a db parser?
Socket, Read, Scan, complete - decode

How quickly are  we able to work with the incoming data?
The goal : parsing overlaps packet receipt (without copy)

- one port, many connection. we should be able to spread them out to a number of endpoints on our system.

* Connections become Shard Local Futures
* The bytes Live in a Bytesmut - Ownership changes hands at the read boundary.
* Parsing starts on every read

### TARmageddon: Reflections on a Multi-Crate CVE

Overview of talk
* Discovery of vuln
* The bug class -- why rust does not help with this class of bugs
* Human angle of maintaining Rust crates
* How to prevent in the future

Discovery timeline
* Working on implementing NVIDIA GPU support
* Was working on benchmarking, pulled an OCI image from the NVIDIA registry
* But pulling the image failed. Inspected the temporary directory and it contained a lot of files that aren't container layers?
* The tokio-tar library was untarring the OCI layers for no apparent reason!
* CVE-2025-62518: a tar boundary-parsing bug within Rust async TAR libraries
  * It allows you to craft a malicious tar that appears safe, but when you use a vulnerable library to untar it, it can
    replace the safe content with some malicious content
  * it impacted `uv`!  And CI pipelines since they deal with tars a lot


Typical tar processing
* There is a 512 byte header, then the file data, then another header, then another file, etc.
* PAX extension: it has its own header.  The problem comes about when you have a parser that does not use PAX but does not also ignore the PAX header

Fork issue
* There is a `tar` crate which is synchronous, not vulnerable to the problem (it had been patched)
* There is a fork of this crate to add async
* Somebody in turn forked `async-tar` to add tokio support
* Then two people forked `tokio-tar`
* When one of these was patched, the forks didn't necessarily get patched! There was no single upstream to patch, so they had to work with all of them.

### Beyond the &: A future for Native Smart Pointers in Rust
Rust is often described as a "library-first language," yet user-defined smart pointers are second-class compared to built-in types like &, &mut, and Box. These native types enjoy special privileges, like integration with the borrow checker, that ordinary library types like Arc, RefCell, and PyRef cannot currently replicate. These limitations make Rust much clunkier than it has to be. They also prevent powerful concepts like in-place initialization from being expressed safely.

This talk presents an update on the "Beyond the &" roadmap, an initiative to bridge these gaps. We explore how exposing the borrow checker's fundamental primitives allows us to generalize standard "superpowers" to user-defined types. We discuss plans to extend the reference model itself by investigating new types like &own and &uninit, which aim to solve long-standing issues with safe in-place construction.

By mapping out these primitives, we enable a future where pointer types like VolatilePtr and CppRef – as well as wrapper types like Cell and MaybeUninit – are as ergonomic to use as builtin types. This creates powerful new patterns for systems programming, interoperability, and abstraction.



